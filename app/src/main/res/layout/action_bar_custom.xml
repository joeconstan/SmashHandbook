<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="wrap_content">



        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="left"
            android:text="Menu"
            android:textColor="#ffffff"
            android:textSize="22dp"
            android:id="@+id/actionbartext"
        />

    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_info_outline"
        android:layout_toRightOf="@+id/actionbartext"
        android:paddingLeft="200dp"
        android:background="@null"
        android:id="@+id/actionbar_info_button"
        />
    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@null"
        android:paddingLeft="12dp"
        android:id="@+id/actionbar_settings_button"
        android:layout_toRightOf="@+id/actionbar_info_button"
        android:src="@drawable/ic_settings_button"
        />



</RelativeLayout>